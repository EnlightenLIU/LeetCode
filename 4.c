//寻找两个有序数组的中位数
#include <stdio.h>
double findMedianSortedArrays(int *nums1, int nums1Size, int *nums2, int nums2Size)
{
    int *p;
    if (nums1 == NULL)
    {
        p = nums2;
    }
    else if (nums2 == NULL)
    {
        p = nums1;
    }
    else
    {
    }
}